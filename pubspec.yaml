name: flutter_raven
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

#Added required Dart and Firebase Dependencies. 
dependencies:
  cloud_firestore: ^2.5.1
  file_picker: ^4.2.2+1
  firebase_auth: ^3.1.0
  firebase_core: ^1.6.0
  firebase_database: ^8.1.0
  firebase_storage: ^10.1.0
  flutter:
    sdk: flutter
  google_fonts: ^2.0.0
  provider: ^6.0.0
  url_launcher: ^6.0.12
  uuid: ^3.0.5
  
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

# The following section is specific to Flutter.
flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/RavenLogo.png
    - assets/RavenLogin.png


 